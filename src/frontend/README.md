# SELAP Frontend

Next.js frontend for the SELAP real estate sales management application.

## Getting Started

```bash
npm install
npm run dev
```

The app runs at `http://localhost:3000` by default.

Copy `.env.example` to `.env.local` and update values when connecting to the backend.
